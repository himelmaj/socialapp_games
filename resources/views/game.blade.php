<x-app-layout>

    <section class="col-span-4 md:col-span-3 mt-10 mx-auto py-5 container">
        <article class="">
            <h3 class="text-xl text-white">Chat For: {{ $game->title }}</h3>
            {{-- <img src="{{ asset('images/' . $game->src) }}" alt="{{ $game->title }}" class="w-full h-full object-cover"> --}}

            {{-- <p class="ml-1 text-sm text-blue-400 mt-2">{{ $user->email." ✨". $user->username }}</p> --}}
            </div>
        </article>

        <article class="">
        </article>
    </section>

</x-app-layout>
